version: 2

models:
  - name: dim_product
    columns:
      - name: product_sk
        description: Chave primaria da dimensão product
        tests:
          - unique
          - not_null
